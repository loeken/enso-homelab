{{ if .Values.openwebui.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "11"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  name: llm-open-webui
  namespace: argocd
spec:
  destination:
    namespace: llm
    server: https://kubernetes.default.svc
  project: default
  source:
    chart: open-webui
    helm:
      values: |
        replicaCount: 1

        # Disable bundled Ollama — we use our external LiteLLM router
        ollama:
          enabled: false

        pipelines:
          enabled: {{ .Values.openwebui.pipelines.enabled }}
          extraEnvVars:
            - name: PIPELINES_API_KEY
              valueFrom:
                secretKeyRef:
                  name: litellm
                  key: LITELLM_MASTER_KEY

        openaiBaseApiUrl: "http://llm-litellm.llm.svc.cluster.local:4000/v1"

        extraEnvVars:
          - name: OPENAI_API_KEY
            valueFrom:
              secretKeyRef:
                name: litellm
                key: LITELLM_MASTER_KEY
          - name: WEBUI_AUTH
            value: "true"
          - name: WEBUI_SECRET_KEY
            valueFrom:
              secretKeyRef:
                name: open-webui
                key: WEBUI_SECRET_KEY
          - name: VECTOR_DB
            value: "qdrant"
          - name: QDRANT_URL
            value: "http://qdrant.llm.svc.cluster.local:6333"
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: llm-postgresql-cluster-app
                key: uri
          - name: TOOL_SERVER_CONNECTIONS
            value: '[{"url":"http://mcp-servers.llm.svc.cluster.local:8000/grafana","auth_type":"none","config":{"enable":true},"info":{"id":"grafana","name":"Grafana"}}]'

        persistence:
          enabled: true
          size: {{ .Values.openwebui.storageSize }}
          storageClass: {{ .Values.openwebui.storageClassName }}

        service:
          type: ClusterIP
          port: 8080

        ingress:
          enabled: {{ .Values.openwebui.enabled }}
          annotations:
            cert-manager.io/cluster-issuer: "{{ .Values.clusterIssuer }}"
            external-dns.alpha.kubernetes.io/hostname: "ai.{{ .Values.domain }}."
            external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
            external-dns.alpha.kubernetes.io/ttl: "120"
            {{- if .Values.openwebui.useAuthelia }}
            nginx.ingress.kubernetes.io/auth-url: "http://authelia.authelia.svc.cluster.local/api/authz/auth-request"
            nginx.ingress.kubernetes.io/auth-signin: "https://auth.{{ .Values.domain }}"
            {{- end }}
          class: "nginx"
          host: ai.{{ .Values.domain }}
          tls: true
          existingSecret: open-webui-tls

    repoURL: https://helm.openwebui.com
    targetRevision: 6.4.0
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{ end }}

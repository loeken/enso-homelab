apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  # # dummy 'source' still required for CRD validation
  # source:
  #   repoURL: ''
  #   path: '.'

  sources:
    - repoURL: https://argoproj.github.io/argo-helm
      chart: argo-cd
      targetRevision: 8.0.17
      helm:
        valueFiles:
          - $values/deploy/terraform/argocd/argocd-values.yaml

    - repoURL: git@github.com:loeken/{{ .Values.githubRepo }}
      targetRevision: HEAD
      ref: values

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
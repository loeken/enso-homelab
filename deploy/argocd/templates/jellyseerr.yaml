{{ if .Values.jellyseerr.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "6"
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  name: jellyseerr
  namespace: argocd
spec:
  destination:
    namespace: media
    server: https://kubernetes.default.svc
  project: default
  source:
    chart: jellyseerr
    helm:
      values: |
        persistence:
          config:
            enabled: {{ .Values.jellyseerr.config.enabled }}
            accessMode: ReadWriteOnce
            size: {{ .Values.jellyseerr.config.size }}
            storageClass: {{ .Values.jellyseerr.config.storageClass }}
        ingress:
          main:
            enabled: {{ .Values.jellyseerr.ingress.enabled }}
            annotations:
              cert-manager.io/cluster-issuer: "{{ .Values.clusterIssuer }}"
              external-dns.alpha.kubernetes.io/hostname: "requests.{{ .Values.domain }}."
              external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
              external-dns.alpha.kubernetes.io/ttl: "120"
              {{- if .Values.jellyseerr.useAuthelia }}
              nginx.ingress.kubernetes.io/auth-url: "http://authelia.authelia.svc.cluster.local/api/authz/auth-request"
              nginx.ingress.kubernetes.io/auth-signin: "https://auth.{{ .Values.domain }}"
              {{end}}
            className: nginx
            hosts:
              - host: requests.{{ .Values.domain }}
                paths:
                  - path: /
                    pathType: Prefix
                    service:
                      port: 5055
                      name: jellyseerr
            tls:
            - secretName: jellyseerrserver-tls
              hosts:
              - requests.{{ .Values.domain }}
    repoURL: https://loeken.github.io/helm-charts
    targetRevision: 2.3.0
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true

{{ end }}
{{ if .Values.wazuh.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wazuh-full-stack
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://morgoved.github.io/wazuh-helm/
    targetRevision: v0.1.4
    chart: wazuh
    helm:
      releaseName: wazuh
      values: |
        global:
          createNamespace: true

        manager:
          enabled: true

        indexer:
          enabled: true

        dashboard:
          enabled: true
          ingress:
            enabled: true
            className: nginx
            annotations:
              cert-manager.io/cluster-issuer: "{{ .Values.clusterIssuer }}"
              external-dns.alpha.kubernetes.io/hostname: "wazuh.{{ .Values.domain }}."
              external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
              external-dns.alpha.kubernetes.io/ttl: "120"
              {{- if .Values.wazuh.useAuthelia }}
              nginx.ingress.kubernetes.io/auth-url: "http://authelia.authelia.svc.cluster.local/api/authz/auth-request"
              nginx.ingress.kubernetes.io/auth-signin: "https://auth.{{ .Values.domain }}"
              {{- end }}
            host: wazuh.{{ .Values.domain }}
            tls:
              - secretName: wazuh-dashboard-tls
                hosts:
                  - wazuh.{{ .Values.domain }}

        certs:
          enabled: true

        agent:
          enabled: true
  destination:
    server: https://kubernetes.default.svc
    namespace: wazuh
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
{{ end }}
name: Destroy k3s

on:
  workflow_dispatch: # Allows manual triggering of the workflow

permissions:
  contents: read

jobs:
  destroy-k3s:
    runs-on: ubuntu-latest
    env:
      TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN_app_terraform_io }}
      TF_WORKSPACE: k3s-prod
      TF_CLOUD_ORGANIZATION: ${{ secrets.TF_CLOUD_ORGANIZATION }}
      TF_CLI_CONFIG_FILE: ${{ github.workspace }}/.terraformrc

    steps:
      - name: Checkout Repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - name: Validate required secrets
        run: .github/scripts/validate-secrets.sh
        shell: bash
        env:
          SSH_USERNAME: ${{ secrets.SSH_USERNAME }}
          SSH_SERVER_ADDRESS: ${{ secrets.SSH_SERVER_ADDRESS }}
          SSH_INTERNAL_ADDRESS: ${{ secrets.SSH_INTERNAL_ADDRESS }}
          SSH_SERVER_PORT: ${{ secrets.SSH_SERVER_PORT }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_TOKEN_app_terraform_io }}
          HOSTNAME: ${{ secrets.HOSTNAME }}

      - name: Setup Terraform
        if: env.SKIP_WORKFLOW != 'true'
        uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4
        with:
          terraform_version: latest

      - name: Set up Terraform CLI configuration
        run: |
          echo "credentials \"app.terraform.io\" {" > .terraformrc
          echo "  token = \"$TF_TOKEN_app_terraform_io\"" >> .terraformrc
          echo "}" >> .terraformrc

      - name: Generate auto.tfvars from secrets
        working-directory: deploy/terraform/k3s
        run: |
          cat <<EOF > github.auto.tfvars
          ssh_username        = "${{ secrets.SSH_USERNAME }}"
          ssh_server_address          = "${{ secrets.SSH_SERVER_ADDRESS }}"
          ssh_server_port             = "${{ secrets.SSH_SERVER_PORT }}"
          ssh_private_key             = "${{ secrets.SSH_PRIVATE_KEY }}"
          internal_ip                 = "${{ secrets.SSH_INTERNAL_ADDRESS }}"
          external_ip                 = "${{ secrets.SSH_SERVER_ADDRESS }}"
          hostname                    = "${{ secrets.HOSTNAME }}"
          argocd_deploy_private_key   = "${{ secrets.ARGOCD_DEPLOY_PRIVATE_KEY }}"
          argocd_repo_name            = "${{ github.repository }}"
          EOF

      - name: Terraform Init
        working-directory: deploy/terraform/k3s
        run: terraform init -input=false

      - name: Terraform Plan Destroy
        working-directory: deploy/terraform/k3s
        run: terraform plan -destroy -input=false

      - name: Terraform Destroy
        working-directory: deploy/terraform/k3s
        run: terraform destroy -auto-approve -input=false

      - name: Cleanup Terraform State
        working-directory: deploy/terraform/k3s
        run: |
          echo "Cleaning up Terraform state files..."
          rm -rf .terraform
          rm -f .terraform.lock.hcl
          rm -f terraform.tfstate
          rm -f terraform.tfstate.backup

      - name: Remove kubeconfig Secret
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_GITHUB_TOKEN }}
        run: |
          echo "Removing KUBECONFIG secret..."
          gh secret remove KUBECONFIG || echo "KUBECONFIG secret not found, skipping."
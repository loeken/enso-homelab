name: Create K3s VMs

on:
  workflow_dispatch:
  issue_comment:
    types: [created]

jobs:
  create-k3s-vms:
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event.issue.pull_request == null && contains(github.event.comment.body, '/install-k3s-vms'))
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run VM Creation Playbook
        run: |
          echo "Hello World - This will create K3s VMs in Proxmox"
          # TODO: Add actual playbook execution here